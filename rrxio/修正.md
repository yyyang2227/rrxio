# 修正方案
- 采用单写者原则：只有 estimator worker 线程能改滤波状态；其他线程只入队。
- 事件队列使用按时间戳排序 + 稳定 tie-break（建议同戳顺序：IMU传播优先，再视觉/雷达更新）。
- 增加watermark 机制：只有当 IMU 覆盖到观测时刻（含安全裕量）才允许该观测出队更新。
- 明确队列溢出策略与诊断计数：禁止静默丢弃，所有 drop/reject 必须可追踪。
- 先在分支做“两阶段迁移”：先保守搬迁执行线程，再放开雷达独立触发，降低一次性改造风险。
